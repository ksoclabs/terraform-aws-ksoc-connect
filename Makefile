CURRENT_WORKING_DIR=$(shell pwd)
TERRAFORM_DOCS_VERSION=v0.16.0

# ============================================================================================================
# Local tasks
# ============================================================================================================

initialise:
	pre-commit install
	pre-commit run -a

# ============================================================================================================
# CI tasks
# ============================================================================================================

run-gitleaks:
	gitleaks --config gitleaks.toml --pretty --verbose --repo-path .

module-docs-check:
	./bin/module-readme-checker.sh
